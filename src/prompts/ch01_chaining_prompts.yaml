promptExtract:
  template: "Extract the technical specifications from the following text:
{textInput}"

promptTransform:
  template: "Transform the following specifications into a JSON object with 'cpu', 'memory', and 'storage' as keys:
{specifications}"
